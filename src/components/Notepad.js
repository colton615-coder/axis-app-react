// src/components/Notepad.js

function Notepad() {
  return (
    <div>
      <h2>Practice Notepad</h2>
      <p>This is where the notepad feature will be built.</p>
    </div>
  );
}

export default Notepad;
